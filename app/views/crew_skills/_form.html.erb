<%= form_for(@crew_skill) do |f| %>
  <% if @crew_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crew_skill.errors.count, "error") %> prohibited this crew_skill from being saved:</h2>

      <ul>
      <% @crew_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :crew_id %><br>
    <%= f.number_field :crew_id %>
  </div>
  <div class="field">
    <%= f.label :skill_id %><br>
    <%= f.text_field :skill_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
