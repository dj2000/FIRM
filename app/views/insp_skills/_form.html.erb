<%= form_for(@insp_skill) do |f| %>
  <% if @insp_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@insp_skill.errors.count, "error") %> prohibited this insp_skill from being saved:</h2>

      <ul>
      <% @insp_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :inspector_id %><br>
    <%= f.number_field :inspector_id %>
  </div>
  <div class="field">
    <%= f.label :skill_id %><br>
    <%= f.text_field :skill_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
