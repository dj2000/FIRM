<% if @proj_sched.errors.present? %>
	$(".modal").html("<%= escape_javascript(render 'crew_inspection') %>");
<% else %>
	$(window.location.replace('/projects/<%= @proj_sched.project_id %>'));
<% end %>