var type = "<%= @user.status.downcase %>";
$('.userlist').html("<%= escape_javascript(render template: 'users/index') %>");
$('#attached_deals_tab > .active').removeClass("active");
$('#attached_deals_tab > .' + type).addClass("active");